pipeline {
    agent any

    environment {
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-credentials')
        GITHUB_CREDENTIALS = credentials('github-token')
    }

    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'main', url: 'https://github.com/jacky4421/pipeline-for-dcoker-hub.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    // ✅ Removed 'sudo' and fixed Docker username
